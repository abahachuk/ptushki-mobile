import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  constainer: {
    flex: 1
  },
  image: {
    width: "100%",
    height: "100%",
    resizeMode: "cover"
  }
});

export default styles;
